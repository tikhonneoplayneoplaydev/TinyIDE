[package]
name = "comet-ide"
version = "0.1.0"
description = "Comet IDE — VS Code-style IDE built with Tauri 2, React and Monaco"
authors = ["tikhonneoplayneoplaydev"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
